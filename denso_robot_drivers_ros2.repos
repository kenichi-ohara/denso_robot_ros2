# Gazebo Classic is not available on ROS 2 Jazzy, and this fork removes the
# Gazebo startup from the launch files, so no external repositories need to be
# imported via vcs on Jazzy.
repositories: {}
